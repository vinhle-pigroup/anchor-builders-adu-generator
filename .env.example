# ===================================
# ANCHOR BUILDERS ADU GENERATOR
# Environment Configuration Template
# ===================================

# Environment Type
NODE_ENV=development

# ===================================
# SERVER-SIDE CONFIGURATION
# (These variables are only accessible on the server)
# ===================================

# PDF Service API Configuration
# CRITICAL: Never prefix server secrets with VITE_
PDF_SERVICE_API_KEY=your-secure-api-key-here
PDF_SERVICE_URL=https://anchor-pdf-service-production.up.railway.app

# CORS Configuration
# Leave empty for same-origin only, or specify allowed origin
API_TRUSTED_ORIGIN=

# Server Port (Railway will set this automatically)
# PORT=8080

# ===================================
# CLIENT-SIDE CONFIGURATION  
# (These variables are bundled into the client code)
# SAFE TO EXPOSE - No secrets here!
# ===================================

# Template Variant Control
VITE_TEMPLATE_VARIANT=NEW-MODERN

# Company Information
VITE_COMPANY_NAME="Anchor Builders"
VITE_COMPANY_WEBSITE="https://www.anchorbuilders.io"
VITE_SUPPORT_EMAIL="support@anchorbuilders.io"
VITE_SUPPORT_PHONE="(555) 123-4567"

# PDF Service URL (public endpoint)
VITE_PDF_SERVICE_URL="https://anchor-pdf-service-production.up.railway.app"

# Feature Flags
VITE_ENABLE_PDF_GENERATION=true
VITE_ENABLE_GOOGLE_MAPS=false

# ===================================
# AUTHENTICATION (OPTIONAL)
# ===================================

# Microsoft Authentication Library (MSAL) Configuration
# Set VITE_REQUIRE_AUTH=true to enable authentication
VITE_REQUIRE_AUTH=false

# Azure AD Application Registration
# Get these from Azure Portal > App Registrations
VITE_MSAL_CLIENT_ID=your-azure-ad-client-id
VITE_MSAL_TENANT_ID=your-azure-ad-tenant-id

# Optional: Specify admin users (comma-separated email addresses)
# If not set, all authenticated users have admin access
VITE_ADMIN_USERS=admin@yourcompany.com,admin2@yourcompany.com

# Optional: Analytics and Monitoring
# VITE_ANALYTICS_ID="your-analytics-id"
# VITE_SENTRY_DSN="your-sentry-dsn"

# ===================================
# DEPLOYMENT NOTES
# ===================================
# 
# Railway will automatically set:
# - PORT (do not override)
# - NODE_ENV=production
#
# You must manually set in Railway dashboard:
# - PDF_SERVICE_API_KEY (get from pdf-service deployment)
# - API_TRUSTED_ORIGIN (if needed for CORS)
#
# SECURITY REMINDER:
# - Never put API keys in VITE_ variables
# - Server secrets must use process.env, not import.meta.env
# - Always use the proxy pattern for external API calls